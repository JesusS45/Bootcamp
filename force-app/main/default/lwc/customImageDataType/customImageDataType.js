/*
 * Author : Jesus Enrique Salazar
 * Description : The component to show a image in a Datatable.
 */
import { LightningElement, api } from 'lwc';

export default class CustomDataTable extends LightningElement {
    // Public Property
    @api url;
    @api altText;
    // Private Property
}